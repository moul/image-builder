#!/bin/bash

. ../ubuntu-trusty/build --source-only

PKGS_INCLUDE="$PKGS_INCLUDE,iotop,iperf,ethstatus,traceroute,tcpdump,busybox,htop,socat,lsof"
NAME=${NAME:-"rootfs-$ARCH-rescue"}

if [ "${1}" != "--source-only" ]; then
    cli $@
fi
